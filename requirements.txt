# Requirements for helper scripts (root level)
# The core pretalx_client.py uses only stdlib (urllib, json, os, sys).
# The following are needed by helper scripts only:

# QR code generation (helper_createRoomLinks.py)
qrcode[pil]>=8.2
